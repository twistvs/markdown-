查看git配置（两种）

```bash
git config  --list

git config --global --list
```

全局设置github代理（只影响github）

```bash
git config http.https://github.com.proxy http://127.0.0.1:7890
```

全局取消github代理

```bash
git config --global --unset http.https://github.com.proxy
```

