#### 暴力检查

substr(起始位置， 从起始位置往后多少个)

```c++
class Solution {
public:
    int strStr(string haystack, string needle) {
        int needleSize = needle.size();
        int haystackSize = haystack.size();

        //依次检查每个子串就可以
        for(int i = 0; i <= haystackSize - needleSize; i++){
            if(haystack.substr(i, needleSize) == needle){
                return i;
            }
        }

        return -1;
    }
};
```

**还可以用KMP算法，但是我不会**
