#### Set Timer by Event

**以时间设置定时器**可以定时触发事件

<img src="../../图片/image-20260420203659932.png" alt="image-20260420203659932" style="zoom:67%;" />

1. **Event**是要触发的事件（一般都是自定义事件）
2. **Time**是触发的间隔时间（如果是delta second那么就是每帧触发一次，可以用这个来模拟tick)
3. **Looping**设置是否循环触发，如果不打勾就只会触发一次