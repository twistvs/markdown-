- 有很多事件需要被**触发**，我们需要考虑触发的**频率**，以及**每次触发的内容**

- tick事件默认是每帧触发一次，通过定时器也可以实现每帧触发一次自定义事件
- 通过设置定时器的timer，我们可以手动控制事件触发的频率

------

#### 例.耐力恢复

1. 设置变量RechargeRate = 0.01，我们让事件每次被触发时，耐力增加0.01
2. 设置变量RechargeTime = 0.2，我们让事件每0.2秒被触发一次
3. 也就是说，一秒会触发五次事件，每次恢复0.01耐力值

