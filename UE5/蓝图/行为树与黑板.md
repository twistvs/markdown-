==行为树与黑板是如何绑定的？==

行为树的细节面板中设置黑板资产，也就是说行为树和黑板是通过**设置**绑定在一起的

==AIcontroller是如何调用黑板的？==

一般情况下，需要在AI控制器蓝图中用“event beginplay"来运行行为树，当运行行为树时，行为树就把自己绑在了AI控制器上，而黑板也会一起被绑上去，此时通过"get blackboard"就可以直接获取到黑板

==AI角色是如何调用黑板的？==

AI角色蓝图需要在细节面板中绑定AI控制器，这样就可以用"get blackborad"获取到黑板